PORT=3000
NODE_ENV=production
APP_BASE_URL=https://hotspot.perumnet.com
# Wajib diisi saat NODE_ENV=production: portal menolak start bila nilai berikut
# masih default atau SESSION_SECRET kurang dari 32 karakter.
# Buat nilai acak dengan: openssl rand -hex 32
SESSION_SECRET=CHANGEME_NOT_A_REAL_SECRET
ADMIN_EMAIL=admin@example.invalid
ADMIN_PASSWORD=CHANGEME_NOT_A_REAL_SECRET
# Mengganti ADMIN_PASSWORD atau ADMIN_EMAIL langsung membatalkan semua sesi
# admin yang sedang berjalan.
ADMIN_SESSION_HOURS=3
# mock untuk pengembangan; redirect untuk gateway. WiFiDog terdeteksi otomatis
# dari gw_address, gw_port, dan token yang diteruskan oleh Reyee.
REYEE_AUTH_MODE=mock
REYEE_USERNAME_PARAM=username
REYEE_PASSWORD_PARAM=password
REYEE_POST_URL_PARAM=post_url
SMTP_HOST=smtp.example.invalid
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=smtp-user@example.invalid
SMTP_PASSWORD=CHANGEME_NOT_A_REAL_SECRET
EMAIL_FROM=smtp-user@example.invalid
EMAIL_REPLY_TO=support@example.invalid
# Isi jika SMTP_HOST memakai IP/private network, agar sertifikat TLS tetap
# diverifikasi menggunakan nama domain mail server.
SMTP_TLS_SERVERNAME=smtp.example.invalid
